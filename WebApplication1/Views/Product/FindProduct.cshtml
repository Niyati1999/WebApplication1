@model Product
@{
    ViewData["Title"] = "FindProduct";
}

<div class="container">
    <div class="card">
        <div class="card-header">Find Product</div>
        <div class="card-body">
            Product Id : 
            <input type="text" placeholder="Enter Product Id here" id="txt_1" />
        </div>
        <div class="card-footer">
            <input type="button" value="Search" id="btn_1" onclick="SearchId()" class="btn btn-primary" />
        </div>
    </div>
</div>
